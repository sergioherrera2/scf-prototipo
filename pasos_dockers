------------------------
PASOS PARA LANZARLO TODO
------------------------
Para borrar los docker que se hayan quedado lanzados se usa: sudo docker rm <2 primeras letras del ID del docker>

NODE-RED: 
	sudo docker run -it -p 1880:1880 -v node_red_data:/data --name mynodered noderedcustom:0.1

INFLUX-DB:
	sudo docker run -p 8086:8086 --name=influxdb --net=my_bridge influxdbcustom:0.1

GRAFANA:
	sudo docker run -p 3000:3000 --name=grafana --net=my_bridge grafanacustom:0.1

TELEGRAF:
	sudo docker run -v /home/jorgemm7/Documentos/GitKraken/scf-prototipo/confs/telegraf.conf:/etc/telegraf/telegraf.conf --net=my_bridge --name=telegraf telegraf
	
MOSQUITTO:
	sudo docker run -p 2883:1883 --name=broker --net=my_bridge eclipse-mosquitto


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

INSTALAR DOCKER: https://docs.docker.com/engine/install/ubuntu/

docker network create my_bridge

docker run -p 8086:8086 --name=influxdb --net=my_bridge influxdb

curl -i http://localhost:8086/query --data-urlencode "q=CREATE DATABASE prototipo"

Hasta donde llega la última coma son los tags. Después están los fields. (Los fields solo nos deja meter 2, por qué)
---------------------------------------------------------------------------------------------------------------------
sudo curl -i 'http://localhost:8086/write?db=prototipo' --data-binary 'nodoIoT,dniCliente="1111111A",nombre="Fernando",apellidos="Vallejo",temperatura=20 calidadAire=20'

docker run -p 2883:1883 --name=broker --net=my_bridge eclipse-mosquitto

docker run -v /home/fernando/Escritorio/Master/SCF/repo/scf-prototipo/confs/telegraf.conf:/etc/telegraf/telegraf.conf --net=my_bridge --name=telegraf telegraf

sudo docker run -v /home/jorgemm7/Documentos/GitKraken/scf-prototipo/confs/telegraf.conf:/etc/telegraf/telegraf.conf --net=my_bridge --name=telegraf telegraf

docker run -p 3000:3000 --name=grafana --net=my_bridge grafana/grafana:6.5.0-ubuntu


---------------------------------------------------------------
sudo docker run -p 8086:8086 --name=influxdb --net=my_bridge influxdbcustom:0.1

sudo docker run -p 3000:3000 --name=grafana --net=my_bridge grafanacustom:0.1


